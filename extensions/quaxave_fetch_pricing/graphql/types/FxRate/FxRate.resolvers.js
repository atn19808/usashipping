module.exports = {
    Query: {
        fxRate: async (_, { filters = [] }) => {
            // TODO:
            return null;
        },
        fxRates: async (_, { filters = [] }) => {
            // TODO:
            return null;
        }
    }
};